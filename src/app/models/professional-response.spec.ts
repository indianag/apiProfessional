import { ProfessionalResponse } from './professional-response';

describe('ProfessionalResponse', () => {
  it('should create an instance', () => {
    expect(new ProfessionalResponse()).toBeTruthy();
  });
});
